// library imports
import React, { FC } from "react";

// component imports
import Text from "./Text";

const Loading: FC = () => {
  return (
    <p>
      <Text text="Loading..." />
    </p>
  );
};

export default Loading;
