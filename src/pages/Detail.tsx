import React, { FC } from "react";

import Title from "../components/Title";

const Detail: FC = () => {
  return (
    <div>
      <Title title="Details" />
    </div>
  );
};

export default Detail;
